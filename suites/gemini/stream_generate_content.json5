// NOTE: This file previously existed as a 0-byte placeholder, which breaks JSON5 parsing
// and prevents suite discovery (CLI + integration tests). Keep it as a valid empty suite
// until real Gemini streaming coverage is added.
{
  provider: "gemini",
  endpoint: "/v1beta/models/gemini-1.5-flash:streamGenerateContent",
  schemas: {},
  base_params: {},
  tests: [],
}
